{
	"name": "dci-app",
	"version": "1.0.0",
	"description": "attendance tracked",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js",
		"server": "nodemon app.js",
		"heroku-postbuild": "cd client && npm install && npm run build",
		"dev": "concurrently --kill-others-on-fail \"npm index\" \"npm client\""
	},
	"author": "fbw6",
	"license": "ISC",
	"dependencies": {
		"bcrypt": "^3.0.6",
		"bcryptjs": "^2.4.3",
		"body-parser": "^1.19.0",
		"cors": "^2.8.5",
		"dotenv": "^8.1.0",
		"express": "^4.17.3",
		"express-jwt": "^5.3.1",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^5.6.10",
		"nodemon": "^1.19.1"
	}
}
